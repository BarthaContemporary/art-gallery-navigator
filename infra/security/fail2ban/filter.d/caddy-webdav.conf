# fail2ban filter for the WebDAV endpoint — matches 401 (auth failure) lines in
# Caddy's JSON access log for files.<domain>. Install to
# /etc/fail2ban/filter.d/caddy-webdav.conf  (see infra/README.md §6d).
[Definition]
failregex = "remote_ip":"<HOST>",.*"status":401
ignoreregex =
