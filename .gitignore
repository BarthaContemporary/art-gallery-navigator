node_modules/
.next/
dist/
.turbo/
.env
.env.local
.env*.local
*.tsbuildinfo
.DS_Store
coverage/
# migration working data (contains commercially sensitive records — never commit)
tooling/migration/data/*
tooling/migration/out/*
!tooling/migration/data/.gitkeep
!tooling/migration/out/.gitkeep
# bootstrap state (live credentials: SSH key, secrets.env — NEVER commit)
infra/scripts/.bootstrap-state/

# Vercel CLI local link artifacts
.vercel/

# Sanity studio build output
apps/web/dist/
apps/web/.sanity/
